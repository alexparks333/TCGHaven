# dependencies
node_modules/
temp-init/

# next.js build output
.next/
tsconfig.tsbuildinfo

# environment / secrets
.env*.local

# generated card catalogs — regenerated via `npm run download-cards` (see CLAUDE.md)
public/data/*.json

# user-uploaded catalog card photos (Admin Catalog "upload a photo" flow) — local machine only
public/uploads/

# sync feature runtime state — regenerated by scripts/sync-runner.mjs
data/backups/
data/sync-status.json
data/sync-run.log
data/last-download-summary.json

# OS
.DS_Store

# Claude Code local settings (not part of the app)
.claude/
